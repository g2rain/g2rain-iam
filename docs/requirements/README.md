# 需求文档

每个需求至少包含目标、非目标、威胁模型、职责边界、协议/数据变化、配置与密钥影响、验收标准、测试、兼容迁移、发布顺序和回滚。

允许状态：`待开发`、`开发中`、`已完成`、`已取消`。执行 `develop` 时优先读取 `docs/project.yaml` 的 `aiCoding.activeRequirement`；为空时只选择唯一 `开发中` 需求。没有或多个候选时停止，不按文件名或修改时间猜测。

`docs/design/` 方案不是自动开发指令。需要实施时，应创建需求或由 `activeRequirement` 明确指向。
