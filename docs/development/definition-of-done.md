# 完成定义

- 需求、威胁模型、非目标和职责边界清楚。
- 没有把 Basis 主数据、Gateway 鉴权或 Shell 治理搬入 IAM。
- 协议、claims、Cookie、错误码、回调和配置变化已同步文档。
- Secret、Token、授权码、state、日志和异常经过安全检查。
- `mvn test` 通过；适用静态检查、集成和失败路径已验证。
- 一次性、TTL、重放、并发、撤销和密钥轮换有测试证据。
- 协作者兼容性、配置迁移、发布顺序和回滚方式已报告。
